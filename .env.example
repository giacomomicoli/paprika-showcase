# HTTP Basic Authentication
# Password should be a bcrypt hash
# To generate a bcrypt hash, use: caddy hash-password --plaintext 'your-password'
# IMPORTANT: In .env files, escape $ with $$ (e.g., $$2a$$14$$hash)
HTTP_AUTH_USER=admin
HTTP_AUTH_PASSWORD=$$2a$$14$$ExampleHashGenerateYourOwn

# Domain configuration (optional, defaults to :80 if not set)
DOMAIN=:80

# Add your other environment variables below
